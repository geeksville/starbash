[repo]
kind = "processed-target"

# This file is auto-generated at the root of a Starbash target processing tree.
# User changes to stage exclusions, overrides, and citation data are preserved.

# The stages used to process this target are recorded below as [[stages]].
# Add `excluded = true` to skip a stage, and uncomment a value under
# [[stages.overrides]] to override a recipe parameter.
[[stages]]

# (Not yet implemented)
# The creator of this processed target and all tools, scripts, and raw images used to produce it 
# are recorded here.
# (if not present this section will be auto generated at processing time)
# the seed for this section is auto copied from userprefs.sb.toml
# The format of this session is compatible with the Citation File Format (CFF)
# see https://github.com/citation-file-format/citation-file-format/blob/main/schema-guide.md
# for details.
# The only difference is to facilitate toml parsing we use an = sign instead of :
# if/when .cff file exports are required we'll do that conversion then.
[processing.citation]
