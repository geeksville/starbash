# Environment vars

There are a few environment vars to change Starbash behavior.  Mostly for developer use.

* STARBASH_TEST_DATA - Test files for integration testing.  Defaults to /test-data
* STARBASH_LOG_DIR - Used to store a few text log files during integration tests.  Defaults to /tmp
