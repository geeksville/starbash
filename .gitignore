*.pyc
__pycache__
images

# Coverage reports
htmlcov/
.coverage
.coverage.*
coverage.xml
junit.xml
# pytest --junit-xml (same as junit.xml; CI regenerates it as an artifact)
pytest.xml

# Doit database files (if created in workspace)
.doit.db*

starbash*.tar.gz
starbash*.whl

.local
.config
.cache
.sass-cache
private/